import createVirtualNodes from '../../src/assembly/create-virtual-node';



describe('createVirtualNodes', ()=> {
  it('should exist', ()=> {
    expect(createVirtualNodes).to.be.a('function');
  });
});